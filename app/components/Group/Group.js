import React, { PropTypes, Component } from "react";
import { View, Text } from "react-native";

export default class Group extends Component {
  static propTypes = {};
  state = {};
  render() {
    return (
      <View>
        <Text>Group Homepage</Text>
      </View>
    );
  }
}
